"use client";

/**
 * Wrapper delgado que mantiene compatibilidad con el import existente en page.tsx.
 * La lógica real vive en shared/ui/ShareButton.
 */
export { default } from "@/shared/ui/ShareButton";
