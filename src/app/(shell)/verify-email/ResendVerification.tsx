/**
 * Movido a shared/ui/ResendVerification.tsx (se reutiliza también en el
 * wizard de /register, paso 2). Re-exportado aquí para no romper el import
 * existente; usa la ruta compartida en código nuevo.
 */
export { ResendVerification } from "@/shared/ui/ResendVerification";
